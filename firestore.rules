rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Cho phép đọc và ghi dữ liệu toàn hệ thống cho người dùng Web App
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
